/*
 * This IIFE is for global variables / constants to be used throughout the site.
 */

const global = {
    /*
     * Setting `debugMode` to `true` will print console messages in the browser
     * to help in debugging and keeping track of what is happening on the page.
     * This should be set to `false` on the public client.
     */
    debug : true
};